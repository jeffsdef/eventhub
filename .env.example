# Segredo para assinar tokens JWT (obrigatório em produção na Vercel)
AUTH_SECRET=altere-para-um-valor-seguro-e-aleatorio

# Opcional: URL externa da API. Se não definida, o app usa /api (mesmo domínio).
# NEXT_PUBLIC_API_URL=
